# ==============================================================================
# IMPORTANT: This file is NO LONGER USED!
# ==============================================================================
# All environment configuration has been moved to the ROOT .env file
# See /HRMS-Portal/.env.example in the project root
#
# The frontend now uses:
# - Local Dev: Vite proxy (configured in vite.config.ts)
# - Docker: Nginx proxy (configured in nginx.conf)
#
# No environment variables are needed for the frontend!
# ==============================================================================

# Legacy variable (not used)
VITE_API_BASE_URL=http://localhost:8080
